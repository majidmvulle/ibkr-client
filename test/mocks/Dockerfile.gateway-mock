FROM python:3.12-alpine

WORKDIR /app

# Install Flask and curl for health checks
RUN pip install --no-cache-dir flask && \
    apk add --no-cache curl

# Copy mock server
COPY mock_gateway.py .

# Expose port
EXPOSE 5555

# Run the mock server
CMD ["python", "mock_gateway.py"]
